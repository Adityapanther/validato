module.exports = {
    isNotEmpty : function isNotEmpty(){
     if (this.data === '' || this.data === undefined) {
         return false
     }else {
       return true
     }
    },

    onlyGmail : function onlyGmail(){
        
    }
}